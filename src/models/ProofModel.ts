export default interface IProof {
  id: number;
  moduleid: number;
  title: string;
  description: string;
  datestart: string;
  deteend: string;
  createdAt: string;
  updatedAt: string;
  status: boolean;
  totalquiz: number;
}
